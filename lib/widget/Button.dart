import 'package:flutter/material.dart';

class buttons extends StatelessWidget {
  buttons({super.key});
  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return ElevatedButton(onPressed: () {}, child: Text('Add Employee'));
  }
}
